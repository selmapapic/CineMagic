@model CineMagic.Dal.Entities.Projection

@{
    ViewData["Title"] = "AddProjection";
}

<h1>Add Projection</h1>

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="AddProjection">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <div class="form-group">
                <label asp-for="ProjectionTime" class="control-label"></label>
                <input asp-for="ProjectionTime" class="form-control" />
                <span asp-validation-for="ProjectionTime" class="text-danger"></span>
            </div>
            @*<div class="form-group">
            @Html.LabelFor(x => Model.Movie, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-5">
                @Html.DropDownListFor(x => Model.Movie, new SelectList(Model.Movie, "Value", "Text"), htmlAttributes: new { @class = "form-control", id = "Country" })
                @Html.ValidationMessageFor(x => x.Movie, "", new { @class = "text-danger" })
           </div>
        </div>*@
            <div class="form-group">
                <label asp-for="MovieId" class="control-label"></label>
                <input asp-for="MovieId" class="form-control" />
                <span asp-validation-for="MovieId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="CinemaHallId" class="control-label"></label>
                <input asp-for="CinemaHallId" class="form-control" />
                <span asp-validation-for="CinemaHallId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="AddProjection" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="HomeAdmin">Back to Administrator Page</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
